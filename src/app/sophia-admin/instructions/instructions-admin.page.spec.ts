import { ComponentFixture, TestBed } from '@angular/core/testing';
import { of } from 'rxjs';
import { SophiaAdminService } from '../sophia-admin.service';
import { InstructionsAdminPage } from './instructions-admin.page';

describe('InstructionsAdminPage',()=>{let fixture:ComponentFixture<InstructionsAdminPage>;let admin:jasmine.SpyObj<SophiaAdminService>;beforeEach(async()=>{admin=jasmine.createSpyObj<SophiaAdminService>('SophiaAdminService',['context','listInstructionSets','createInstructionSet','createInstructionRevision','approveInstructionRevision']);admin.context.and.returnValue(of({principal:{tenantId:'tenant-1',identityUserId:'editor',role:'configuration_editor',permissions:['instructions.read','instructions.edit']}}));admin.listInstructionSets.and.returnValue(of({platformSafetyPolicyVersion:'sophia-safety-1',instructionSets:[{instruction_set_id:'set-1',instruction_key:'neutral-assistant',created_at:'2026-01-01T00:00:00Z',revisions:[{instructionRevisionId:'revision-1',revision:1,status:'draft',content:'Hello {{customerName}}',variableSchema:{type:'object',properties:{customerName:{type:'string'}},additionalProperties:false},createdByIdentity:'editor',createdAt:'2026-01-01T00:00:00Z'}]}]}));await TestBed.configureTestingModule({imports:[InstructionsAdminPage],providers:[{provide:SophiaAdminService,useValue:admin}]}).compileComponents();fixture=TestBed.createComponent(InstructionsAdminPage);fixture.detectChanges();});it('separates tenant drafts from immutable platform safety and live releases',()=>{const text=(fixture.nativeElement as HTMLElement).textContent||'';expect(text).toContain('sophia-safety-1');expect(text).toContain('Not tenant editable');expect(text).toContain('draft');expect(text).toContain('Drafts and approvals');});});
